{
  "name": "ez2gm-cloudflare-deploy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "worker:check": "node --check api-worker/src/index.js",
    "worker:deploy": "wrangler deploy --config wrangler.worker.toml",
    "pages:deploy": "wrangler pages deploy outputs/ez2gm-admin --project-name ez2game1"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
